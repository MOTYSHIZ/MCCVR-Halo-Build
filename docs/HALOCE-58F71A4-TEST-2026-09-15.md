# CE 58f71a4 headset feedback - September 15, 2026

The user confirms both CE graphics modes enter VR and switch successfully in
the first run. Original's camera, hands and overall presentation are excellent;
preserve that base. Original has no visible crosshair. Anniversary's crosshair
appears fixed to the screen, its HUD adjustment needs checking, hands/weapons
flicker dark or disappear, and its image is noticeably softer. On a second MCC
launch Anniversary becomes black while switching back to Original still works.
The desktop appeared partly static/black. Nucleus Co-op was mentioned as an
installed application, but the user explicitly says to disregard it. No causal
link to that application or a second player is established.

The new request explicitly adds CE world collision and physical melee to this
candidate. It supersedes the earlier CE contact exclusion. Continue to a new
test build ZIP and matching source ZIP, package without `-Install`, deliver in
chat, and wait for testing. No MCC launch, installation, game-folder writes,
PR or publication. Both editions remain supported. This is partial CE headset
success, not cumulative acceptance; the accepted pointer stays `4e01f28`.

## Exact preserved evidence

Both logs identify source `58f71a4af1808f144b63f10ca244df6584dcebf2`, Steam,
SteamVR/OpenXR 2.17.9, Oculus-family headset, 90 Hz panel. The precise model is
not identified by these logs. Their files are retained under
`out/test-runs/58f71a4-ce-feedback-20260915/`:

- `first-run.log`, PID 19516, starts 17:49:29.800, SHA-256
  `5E1FCDC8DF3ECF3E5F6121760E5C87A30E2B485400B74C779001FDB82AA19024`.
- `second-run.log`, PID 16064, starts 17:53:54.723, SHA-256
  `1976D098D4CB47DD181BBA6E8A029878B50BF6520E9077E09FA164D7E018CFA3`.

## What the logs prove

First-run Anniversary frames show eyeMask=3, both native players=0 and complete
depth/scene/shading masks. Second-run Anniversary repeatedly shows copy-shape
failure, eyeMask=1, while those three consumer masks remain 3 and players=0/0.
By 17:55:03 it records 939 descriptor misses. Original subsequently completes
hundreds of pairs without further descriptor misses. The failure is downstream
of both Anniversary camera consumers, not evidence of a real second player.
The old copy diagnostics did not save the rejected source descriptor/arguments;
they do not distinguish every cause of copy-shape rejection.

Anniversary's source raster is 2912x1050; Original uses 2912x2100. Thus the
reported sharpness difference has a measured native allocation counterpart.
Increasing an output texture without increasing native scene/depth allocation
would not restore the missing rendered detail.

Successful copied-bone scale counts have zero stock fallbacks. Material lens
policy refusals continue alongside successful applications; capture counters
and held-art flags alone do not establish that a nonblank reticle was rendered.
The corrected candidate must be tested in the headset; local fixtures cannot
prove the entire second-launch or graphics-switch report resolved.
