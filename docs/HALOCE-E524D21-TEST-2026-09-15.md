# CE scale progress and stability feedback - September 15, 2026

## Identity and user result

Both pasted logs identify source `e524d219c91015aa4f97193fedb96e43ae009eac`,
Steam, SteamVR/OpenXR 2.17.9, `SteamVR/OpenXR : oculus`, 90 Hz panel.
The exact headset model is not named. Package DLL SHA-256 is
`8E31A5CE559EBE6B71751796E5E8CEA08F5324D2584ACB2F8244827B608DEBD3`;
the installed DLL was not independently read in this session.

Preserved verbatim in `out/test-runs/e524d21-ce-refinement-feedback-20260915/`:

| File | SHA-256 |
| --- | --- |
| `run-152646.log` | `1D13C9E9065B6216ADD8B9D13F691D3B3A4EB6410137D44357FB166F01D68418` |
| `run-152917.log` | `A000DBC5DE58A377917146090CE09F1CA7201E7807B9106AE10792EB1D442C1C` |
| `original-stretched-arms.jpg` | `81AB3156027A524119B2F5F001D10D87E27A3CD5CD879C43148A873800886A17` |

The user confirms successful injection in both graphics modes, excellent gun
scale in both, and working Original HUD. Preserve that progress. Reported
defects are stretched Original arm triangles attached to the gun, flickery
Original startup until an Anniversary roundtrip, flickering Anniversary hands,
missing Anniversary HUD, and a head-following menu after leaving CE with a
possible crash when entering another title. The screenshot visibly supports
the stretched-triangle report; it does not alone establish its native cause.

The latest request explicitly authorizes cumulative CE Original/Anniversary
refinement through new build AND matching source ZIPs. Halo 3 is to be left
alone. The briefly mentioned extra task was withdrawn. This replaces older
packaging holds; package without `-Install`, deliver both, then wait. No game
launch, install, game-folder changes or PR. All standing/deferred requests
remain preserved, and accepted cumulative source stays `4e01f28`.

## Log findings

| Final counters | 15:26 run | 15:29 run |
| --- | ---: | ---: |
| All captured CE pairs | 3,071 | 1,618 |
| Original pairs / drops | 2,689 / 6 | 884 / 11 |
| Original stock render entries | 1,171 | 100 |
| Original source misses | 0 | 0 |
| Tracked palettes applied / stock | 5,513 / 0 | 2,073 / 416 |
| Anniversary HUD draws / fallback | 0 / 768 | 0 / 1,472 |

Every HUD refusal reports `primary-eye-ownership` (failure 11). The first run's
Original stock counter grows by roughly 30 every two seconds before the first
graphics switch, alongside about 60 successful stereo pairs. After returning
from Anniversary it stays at 1,171 while pairs keep advancing. Original lens
fallback grows during those initial stock intervals. These counters identify
intermittent stock rendering, not missing output descriptors. The production
interleaving regression described in the startup evidence reproduces one
specific cause without claiming that every stock call in the log has it.

Both runs enter native pause, then lose the camera heartbeat and detach CE
presentation. CE remains detected with the same generation afterward. In the
first run disarm is 15:28:54.799, detach 15:28:56.083, and Present stall
15:29:04.904. In the second: 15:30:39.168, 15:30:40.359, 15:31:51.906.
Worker logging continues after each stall. No crash exception, fault address
or stack is present. The log's parenthetical attribution of a stall to the
game is a diagnostic string, not proof that mod code cannot cause it.

There is no matching CE pre-install log in `out/deploy-backups` for these
package-only handoffs. Comparison uses preserved `a2b526a` feedback: both
graphics modes already rendered, but scale/pause refinements were still needed.
The new results establish scale and Original HUD progress, not complete CE
acceptance. No Halo 3 injection redesign follows from this report.

Original steady render-window p95 in the first run is about 10-12 ms;
Anniversary reaches 35.46 ms in the window ending 15:28:33.682. These timings
do not establish 90 distinct game frames per second or isolate mod overhead.
No speculative performance tuning is justified by these logs alone.
