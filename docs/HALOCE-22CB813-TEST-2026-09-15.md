# CE 22cb813 headset feedback - September 15, 2026

The user confirms an excellent working CE Original and Anniversary VR base:
both inject and run smoothly, image quality is equivalent, and muzzle flashes
now align correctly. Original's HUD remains good. Preserve these results.
The remaining reported defects are the temporary-looking reticle in both
modes, missing Anniversary HUD, and contact affecting hands but not gun meshes.
The request is to restore native weapon reticles, fix Anniversary HUD, and
extend world contact and physical strikes to every stock CE weapon mesh.
Halo 2 and Halo 4 are cited as earlier native-reticle fixes to learn from.

This is feature-specific CE acceptance, not acceptance of the unfinished HUD,
reticles or weapon contact, nor a new all-title regression result. The cumulative
accepted source remains `4e01f28`. Package a new build and matching source ZIP
without `-Install`, deliver both in chat, and wait for the user's testing.
No installation, game-folder writes, MCC launch, PR or publication is authorized.

## Preserved evidence

- Source: `22cb8134e02ea84fc455d4ef3a2f284268ffe49b`, PID 28372.
- Steam; SteamVR/OpenXR 2.17.9; Oculus-family headset; panel 90 Hz.
  The log does not name the exact headset model.
- Log: `out/test-runs/22cb813-ce-feedback-20260915/user.log`.
- Log SHA-256:
  `5056F054F64700856FE5C902FAAB51D3320B72A31E3C216D9DEA735CD880540D`.
- Prior candidate manifest/hashes: `candidate-handoff.json` in that directory.
  This records packaged DLL SHA-256
  `C86ABD3360B03426414A7FE347743B0C21186C8C37456684684FFCD322B540AD`;
  the supplied text does not independently verify the installed DLL hash.
- Comparison with both preserved `58f71a4` logs: `log-comparison.json` in
  the same directory. No newer CE deployment backup exists; package-only
  delivery has left the older September 5 backups unrelated to this test.

## What the log establishes

All 37 reticle telemetry windows report zero uploads, and measured artwork
never becomes available. Native capture counts increase and a controller marker
is admitted. Capture execution alone therefore does not prove visible artwork.
Anniversary HUD reaches 2,464 reported draws without a reported HUD failure,
despite the user's missing-HUD result. The actual source pixels and their
destination need verification; these counters cannot establish visibility.

Native Anniversary allocation reports 2912x2100 per eye and a 4200-pixel packed
height, with no allocation failures. Both eye copies have complete proof masks.
The earlier second-run copy-shape failures are absent from this supplied run;
that does not independently establish every possible relaunch scenario.

Enabled contact reaches 11,746 queries, 176 contacts and 575 corrections;
physical melee reaches 1,745 queries, 28 contacts and three right-hand
applications. These are native execution counters, not proof that gun surfaces
participate. The user specifically confirms hand contact and reports missing
gun contact. No contact exceptions or fault flags appear.

The user's smoothness and equal-quality assessment is the headset result.
The timing log still contains missed display deadlines, so this report does
not claim measured native 90 FPS or universal performance acceptance.
